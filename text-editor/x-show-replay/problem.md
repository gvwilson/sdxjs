Add a command to show the commands currently recorded for replay in the status bar.
Regular characters should be shown as themselves,
while control characters should be shown as `^K`, `^U`, and so on.
