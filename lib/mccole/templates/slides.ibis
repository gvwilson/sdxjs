<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "header.html" %}
    <title>{{site.title}}: {{node.title}}</title>
  </head>
  <body>
    <textarea id="source">

class: title-slide

<div class="centered title">
  <p>{{site.title}}</p>
  <h1>{{node.title}}</h1>
  <p>
    <img class="slide-logo" src="@root/{{site.logo}}" alt="logo" />
  </p>
</div>

---

{{node.html}}

    </textarea>
    {% include "remark.html" %}
  </body>
</html>
