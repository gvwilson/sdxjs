\documentclass[krantzl]{tex/krantz}

% Language rules.
\usepackage[english]{babel}

% Font encoding.
\usepackage[T1]{fontenc}

% Show page boundaries.
% \usepackage[showframe]{geometry}
\usepackage{geometry}

% Some special symbols.
\usepackage{amssymb}

% Format code listings.
\usepackage{listings}
\lstset{basicstyle=\small\ttfamily,upquote=true,literate={✓}{{\checkmark}}1}

% Mark keystrokes.
\usepackage{keystroke}

% Include images.
\usepackage{graphicx}

% Make description items cross-referenceable
\def\namedlabel#1#2{\begingroup
    #2%
    \def\@currentlabel{#2}%
    \phantomsection\label{#1}\endgroup
}
\makeatother

% Use numbers for nested lists all the way down and adjust indent.
\usepackage{enumitem}
\setlist[enumerate,1]{label=\arabic*., ref=\arabic*, leftmargin=*}
\setlist[enumerate,2]{label=\arabic*., ref=\arabic*, leftmargin=*}
\setlist[enumerate,3]{label=\arabic*., ref=\arabic*, leftmargin=*}

% Adjust indent for bullet lists.
\setlist[itemize,1]{label=-, leftmargin=*}
\setlist[itemize,2]{label=-, leftmargin=*}
\setlist[itemize,3]{label=-, leftmargin=*}

% Allow multi-page tables.
\usepackage{longtable}

% Center captions.
\usepackage[center]{caption}

% Mark table headers.
\newcommand{\tablehead}[1]{\underline{#1}}

% Figures.
\newcommand{\figimg}[4]{\begin{figure}%
\centering%
\includegraphics[width=\textwidth]{#2}%
\caption{#3}%
\label{#1}%
\end{figure}}

\newcommand{\figpdf}[4]{\begin{figure}%
\centering%
\includegraphics[scale={#4}]{#2}%
\caption{#3}%
\label{#1}%
\end{figure}}

% Cross-references.
\newcommand{\appref}[1]{Appendix~\ref{#1}}
\newcommand{\chapref}[1]{Chapter~\ref{#1}}
\newcommand{\figref}[1]{Figure~\ref{#1}}
\newcommand{\tblref}[1]{Table~\ref{#1}}

% Glossary items and references.
\newcommand{\glossitem}[2]{\item[\hypertarget{#2}{#1}:]}
\newcommand{\glossref}[2]{\textbf{#1}}

% Asides
\usepackage[framemethod=default]{mdframed}

% temp box
\newmdenv[skipabove=7pt,
skipbelow=7pt,
rightline=true,
leftline=true,
topline=true,
bottomline=true,
innerleftmargin=5pt,
innerrightmargin=5pt,
innertopmargin=0pt,
leftmargin=0cm,
rightmargin=0cm,
linewidth=.5pt,
innerbottommargin=0pt]{tBox}

\newtoggle{inshade}
\togglefalse{inshade}

\pretocmd{\footnote}{\iftoggle{inshade}{\stepcounter{footnote}}{\relax}}{}{}

\newcommand{\callouttitle}[1]{\begin{center}{#1}\end{center}\vspace{\baselineskip}}
\newcommand{\fixme}[1]{\begin{mdframed}\begin{quotation}FIXME: {#1}\end{quotation}\end{mdframed}}
\newenvironment{callout}{\begin{tBox}\begin{quotation}\toggletrue{inshade}\renewcommand{\thempfootnote}{\arabic{footnote}}}{\vspace{0.5\baselineskip}\end{quotation}\end{tBox}\togglefalse{inshade}}
\newenvironment{hint}{\begin{mdframed}\begin{quotation}}{\end{quotation}\end{mdframed}}
\newlength{\tempindent}
\newenvironment{unindented}{%
  \setlength{\tempindent}{\parindent}%
  \setlength{\parindent}{0pt}%
}{%
  \setlength{\parindent}{\tempindent}%
}

% Unicode characters.
\usepackage{newunicodechar}

% Don't indent footnotes.
\usepackage[hang,flushmargin]{footmisc}

% URLs as footnotes.
% Always load 'hyperref' last (see link below for explanation).
% https://tex.stackexchange.com/questions/16268/warning-with-footnotes-namehfootnote-xx-has-been-referenced-but-does-not-exi
\newcommand{\hreffoot}[2]{{#1}\footnote{\href{#2}{#2}}}
\usepackage{hyperref}

\begin{document}

\title{Software Tools in JavaScript}
\author{Greg Wilson}
\maketitle

\frontmatter
\tableofcontents

\mainmatter
